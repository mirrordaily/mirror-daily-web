import { MEGABYTES } from './storage-unit'
export const AVAILABLE_VIDEO_MIME_TYPE = ['video/mp4', 'video/quicktime']

export const AVAILABLE_IMAGE_MIME_TYPE = ['image/jpeg', 'image/png']

export const MAX_VIDEO_SIZE = MEGABYTES * 30

export const MAX_IMAGE_SIZE = MEGABYTES * 2
